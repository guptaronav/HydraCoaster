#pragma once

// Copy this file to secrets.h and fill in your values.
// secrets.h is gitignored — never commit real credentials.

// Wi-Fi
#define WIFI_SSID     "YourNetworkName"
#define WIFI_PASSWORD "YourNetworkPassword"

// OpenWeatherMap — https://openweathermap.org/api (free tier works)
#define OWM_API_KEY "your_32char_api_key_here"
#define OWM_LAT     "37.7749"    // decimal degrees, e.g. San Francisco
#define OWM_LON     "-122.4194"

// Optional: unicast the POC sample stream to this host instead of broadcast.
// Needed on networks with client isolation that drop broadcast packets.
// #define HYDRA_HOST_IP "192.168.1.100"
